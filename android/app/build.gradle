plugins {
    id "com.android.application"
    id "kotlin-android"
    id "dev.flutter.flutter-gradle-plugin"
}

android {
    namespace = "com.example.clusterapplication"
    compileSdk = 34 // Ensure this is consistent with your Flutter version

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_1_8
        targetCompatibility = JavaVersion.VERSION_1_8
    }

    kotlinOptions {
        jvmTarget = JavaVersion.VERSION_1_8
    }

    defaultConfig {
        applicationId = "com.example.clusterapplication"
        minSdk = 21 // Set to a value compatible with your app
        targetSdk = 34 // Set to a value compatible with your app
        versionCode = 1
        versionName = "1.0"
    }

    buildTypes {
        release {
            signingConfig = signingConfigs.debug // Replace with your release signing config
        }
    }
}

flutter {
    source = "../../.."
}
